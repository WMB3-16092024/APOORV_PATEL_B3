//Declare a variable `city` using `let` and assign it the value `"Mumbai"`. 
//Change the value to `"Delhi"`. Print the value after each assignment.
let city = "mumbai"
console.log(city)
//code will print mumbai
city = "delhi"
console.log(city)
//code will print delhi
