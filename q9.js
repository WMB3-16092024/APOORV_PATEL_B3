/**Math Power and Modulus:**
   Write code to:
   - Declare `base = 3` and `exponent = 4`.
   - Calculate and print `base` raised to the power of `exponent`.
   - Find the modulus of `13` divided by `5` and print the result.*/
   let base = 3
   let exponent = 4
 let power = exponent**base
 console.log({power})
 //code will print 64
 let num1 = 13
 let num2 = 5
 let modulus = num1 / num2
 console.log({modulus})
 // code will print 2.6

