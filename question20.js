/**Rewrite the Code:**
Modify the code to use `const` instead of `let` where possible:
```javascript
let city = "Bhopal";
city = "Indore";
console.log(city);
in const we cannot reassign the value
```*/
