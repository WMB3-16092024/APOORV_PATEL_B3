/**Math Operations in Functions:**
   Write a function `calculatePower` that:
   - Takes two arguments: `base` and `exponent`.
   - Returns the result of `base` raised to the power of `exponent`.
   Call the function with `2` and `3` and print the result.*/
   function calculatePower (exponent , base){
      return exponent**base
   }
   let power = 3**2
   console.log(power)
   //code will print 9