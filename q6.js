/**Equality Operators:**
   Write code to declare two variables:
   - `a = 50` (number).  
   - `b = '50'` (string).  
   Use `==` and `===` to compare `a` and `b` and print the results.*/
   let a = 50
   let b = '50'
   console.log(a==b)
   //true
 console.log(a===b)
 //false  