/**String Manipulation with Functions:**
   Write a function `greetUser` that:
   - Takes `name` as input.
   - Returns `"Hello, [name]!"`.
   Call the function with `"WebMaster"` and print the result.*/
   function greetUser(name){
    return name

   }
   let name = "webmaster"
   console.log(name)
   //code will print webmaster
