/**Nested Ternary Operator:**
What will the following code print?
```javascript
let age = 16;
const message = age >= 18
    ? "Adult"
    : age >= 12
    ? "Teenager"
    : "Child";
console.log(message);*/
let age = 16;
const message = age >= 18
    ? "Adult"
    : age >= 12
    ? "Teenager"
    : "Child";
console.log(message)
//code will print "Teenager"
