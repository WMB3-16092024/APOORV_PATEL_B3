//Declare variables for `length` and `width` using `let`.
// Assign values to them and calculate the area of a rectangle using these variables. Print: "The area of the rectangle is [area]."
let length = "5"
let width = "5"
console.log(`the area of rectangle`,length*width)
