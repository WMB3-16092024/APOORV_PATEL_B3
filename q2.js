/**Boolean Conversion:**
   Write code to convert the following values to Boolean using `Boolean()`:
   - An empty string (`''`).
   - A non-empty string (`'hello'`).
   - The number `0`.  
   Print the results.*/
   let emptystring = Boolean('')
   console.log(emptystring)
   //code will print false
   let nonemptystring = Boolean(`hello world`)
   console.log(nonemptystring)
   //code will print true
   let thenumber = Boolean (0)
   console.log(thenumber)
   //code will print false